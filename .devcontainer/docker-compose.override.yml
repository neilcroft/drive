services:
  app-dev:
    user: root
    environment:
      HOME: /tmp/dev-home
    entrypoint: []
    command:
      - sh
      - /devcontainer/setup.sh
    volumes:
      - ..:/workspace:cached
      - .devcontainer/setup.sh:/devcontainer/setup.sh:ro
